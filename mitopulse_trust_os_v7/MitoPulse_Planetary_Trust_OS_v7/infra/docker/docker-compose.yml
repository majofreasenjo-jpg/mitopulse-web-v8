services:

  gateway:
    build: ../../services/gateway
    ports: ["8080:8080"]
    depends_on:
      - presence_engine
      - risk_engine
      - ai_detection
      - cluster_engine
      - planetary_simulator

  presence_engine:
    build: ../../services/presence_engine
    ports: ["8081:8081"]

  risk_engine:
    build: ../../services/risk_engine
    ports: ["8082:8082"]

  ai_detection:
    build: ../../services/ai_detection
    ports: ["8083:8083"]

  cluster_engine:
    build: ../../services/cluster_engine
    ports: ["8084:8084"]

  planetary_simulator:
    build: ../../services/planetary_simulator
    ports: ["8090:8090"]

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: mitopulse
      POSTGRES_PASSWORD: mitopulse
      POSTGRES_DB: mitopulse
    ports: ["5432:5432"]

  redis:
    image: redis:7
    ports: ["6379:6379"]
