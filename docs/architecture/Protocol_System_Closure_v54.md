# Protocol System Closure v54

Flow:
input -> event log -> identity -> trust -> risk -> validation -> policy -> action -> recovery -> snapshot -> replay -> federation

## Persistent objects
- events
- evaluations
- votes
- challenges
- policies
- federation anchors
- federation exchanges

## UI target
The dashboard becomes a protocol console and control surface.
